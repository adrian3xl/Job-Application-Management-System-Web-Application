@model IEnumerable<JobApp_Web_.Models.Vacancy_ApplicationVM>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Resume_request_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Vacancy_request_id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Application_status)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Resume_request_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Vacancy_request_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Application_status)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.Id">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.Id">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.Id">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
